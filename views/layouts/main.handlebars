<!DOCTYPE html>
<html lang='en'>
<head>
  <meta charset='UTF-8' />
  <meta http-equiv='X-UA-Compatible' content='IE=edge' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel='stylesheet' href="/css/style.css">
  <title>{{title}}</title>
</head>

<body>
  <header>
    <nav class="d-flex col-12 justify-content-between p-2 bg-secondary">
      <div class="col-12 text-center">
        <a href="/"><button class='btn btn-about-us col-10 col-lg-2 m-1 my-lg-1'>About Us</button></a>
        {{#if logged_in}}
        <a href="/home"><button class='btn btn-home col-10 col-lg-2 m-1' id="home">Home</button></a>
        <a href="/postjob"><button class='btn btn-postjob col-10 col-lg-2 m-1'>Post Job</button></a>
        <a href ="/logout"><button class='btn btn-logout col-10 col-lg-2 align-right m-1' id="logout">Log Out</button></a>
        {{else}}
        <a href="/login"><button class='btn btn-login col-10 col-lg-2 align-right m-1'>Log In</button></a>
        <a href="/signup"><button class='btn btn-signup col-10 col-lg-2 align-right m-1'>Sign Up</button></a>
        {{/if}}
      </div>
    </nav>
  </header>
  <main id="swup" class="transition-fade">
  {{{body}}}
  </main>  
  {{#if logged_in}}
  {{!-- <script defer src="/swup/dist/swup.min.js"></script>
  <script defer src="/js/pageAnimation.js"></script> --}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>